# dist/ is deliberately committed — the README renders the diagram, and CI
# fails if it goes stale. Do not add it here.

.DS_Store
*.tar.gz
threatcl

# Negative-test output. Regenerated by 'make evidence' and published by CI as
# a build artifact, so there is nothing to commit.
/evidence/

# Local development material: certificates, keys and token stores generated by
# the "Run the services" section of the README. No key material belongs in
# this repository.
/dev/
